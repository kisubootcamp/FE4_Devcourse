{const multiplyArrays = (a: number[], b: number[]): void => {
  a.forEach((num) => {
    let a1 = num;
  });
  b.forEach((num) => {
    let b1 = num;
  });
  console.log(a * b)
};
const result = multiplyArrays([1, 2, 3], [4, 5, 6]);
console.log(result); // [4, 10, 18]
}
