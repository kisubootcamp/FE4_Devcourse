{
  // 함수
  // 매개 변수와 반환 값의 타입을 지정
  function greet(): void {
    console.log('Greet!');
  }

  greet();

  function sum(n1: number, n2: number): number {
    return n1 + n2;
  }
  function printValue(value: string): void {
    console.log(value);
  }
  function add(numbers) {}
  
}
