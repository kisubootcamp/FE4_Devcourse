{
  // 01
  const str: string = "Hello";
  console.log(str);

  // 02
  const num: number = 42;
  console.log(num);

  // 03
  const bool: boolean = true;
  console.log(bool);

  // 04
  const numArr: number[] = [1, 2, 3];
  console.log(numArr);

  // 05
  const strArr: string[] = ["안녕하세요", "이것은", "과제 풀이입니다"];
  console.log(strArr);

  // 06
  const tupArr: [number, string] = [7, "행운의 숫자"];
  console.log(tupArr);

  // 07
  const nul: null = null;
  console.log(nul);

  // 08
  const und: undefined = undefined;
  console.log(und);

  // 09
  // const obj: object = ["Jiyoon Lee", 29];
  const obj: { name: string; age: 29 } = { name: "Jiyoon", age: 29 };
  console.log(obj);
}
