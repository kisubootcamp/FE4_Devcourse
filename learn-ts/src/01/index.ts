// 타입스크립트
// 자바스크립트 + 타입을 시스템을 추가한 언어, 정적 타입 시스템
// 자바스크립트는 동적 타입 시스템 언어로 할당과 동시에 타입이 결정됨
// string, number, boolean, object, array, symbol, tuple, undefined, null, any, bigint

{
  let name: string = "jungyu";
  let age: number = 20;
  let bool: boolean = true;

  let obj: object = {};
  let arr: [] = [];

  let sym: symbol = Symbol("a");
  let und: undefined = undefined;
  let nul: null = null;
  let big: bigint = 100n;
}
