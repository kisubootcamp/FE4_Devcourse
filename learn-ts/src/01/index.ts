// ts -> js 변환 하는 작업 == 트랜스 컴파일(= 트랜스 컴파일링)
// 트랜스 컴파일러 (= 트랜스 컴파일을 수행하는 도구)

let num1: number = 10;
console.log(num1);

const str = "Hello, World!";
console.log(str.replaceAll(" ", ""));

// 기본 타입
// string, number, boolean, null, undefined, symbol, bigint, object, Array, any, tuple
