// let num1: number = 10;
// console.log(num1);
// const str = "Hello Jieun!";
// console.log(str.replaceAll(" ", ""));

// 타입 스크립트
// 자바스크립트 + 타입 시스템을 추가한 언어
// 타입스크립트 기본타입
// string, number, boolean, object,
// array, typle, undefined,null, any, bigInt, symbol
// 타입스크립트는 결국 값의 타입을 지정해주는 것
// 값에 대한 타입을 지정하는 방법이 다양함;;;;

{
  let name: string = "jieun";
  let age: number = 20;
  let bool: boolean = true;
  let obj: object = {}; // 나중나중
  let arr: [] = [];
  let sym: symbol = Symbol("a");
  let nul: null = null;
  let und: undefined = undefined;
  let big: bigint = 100n;
}
