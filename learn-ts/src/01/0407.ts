// 오늘 배운 내용
// 1. 깃 풀리퀘스트 하는법
// 2. 타입스크립트 쓰는법

// 타입스크립트 : 자바스크립트 + 타입 시스템을 추가한 언어
// 타입스크립트는 결국 값의 타입을 지정해주는 것.

// string, number, boolean, object, array, symbol, tuple, undefined, null, any, bigint

{
    let name:string="youjin";
    let age:number=430;
    let bool:boolean=true;
    
    let obj: object={}; // 나중에 자세히
    let arr:[]=[]; // 나중에 자세히
    
    let sym:symbol=Symbol("a");
    let und:undefined=undefined; // 작성할 일 없다.
    let nul:null=null;
    let big:bigint=100n;
}