//타입 추론
// 변수에 할당 되는 갑슬 보고 타입스크립트 컴파일러가 타입을 추론해주는 현상

{
  let num = 10;
  let name = "hello";
  let bool = true;
  let unde = undefined;
  let nul = null;
  let symbol = Symbol("a");
  let numArr = [1, 2, 3];
  let obj = { name: "sucoding", age: 20 };
  let big = 100n;
}
