{
    // 리터럴 타입 -> 값 자체를 타입으로 사용하는것
    const num = 10;
    const str = 'A';

    let num2: 5 = 5;
    // num2 = 1; 값으로 지정했기떄문에 5말고 다른것은 못들어간다.

    let bool: true = true;
    bool = true;

    const obj = { name: 'sucoding' };
    obj.name = 'kisu';
    const arr = [1, 2, 3];
}
