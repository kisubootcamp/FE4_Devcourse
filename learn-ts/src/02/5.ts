{
  // 리터럴 타입 -> 값 자체를 타입으로 사용하는 것
  const num = 10;
  const str = "A";
  const bool = true;
  let bool1 = true;

  const obj = { name: "jungyu" };
  obj.name = "park";
  const arr: number[] = [1, 2, 3];
}
