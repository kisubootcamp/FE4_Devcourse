// 자바스크립트 + 타입 (자바스크립트에 타입 시스템을 추가한 언어)
// string, number, boolean, object, array, tuple, symbol, undefined, null, any, bigint
{
    let name: string = "Jeong";
    let age : number = 20;
    let bool : boolean = true;
    let obj : object = {};
    let arr : [] = [];
    let sym : symbol = Symbol("a");
    let und : undefined = undefined;
    let nul : null = null;
    let big : bigint = 100n;
}

// 자바스크립트 -> 동적 타입 시스템
// 타입스크립트 -> 정적 타입 시스템