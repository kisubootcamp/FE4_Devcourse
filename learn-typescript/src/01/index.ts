/* 타입스크립트 */
// 자바스크립트(동적 타이핑) + 타입 시스템을 추가한 언어 : 정적 타이핑
// 때문에 자바스크립트의 문법 먼저 공부해야 함!

// type : string, number, boolean, object, array, symbol, tuple, undifined, null, any, bigint ...
{
    let str: string = "강하영";
    let num: number = 25;
    let bol: boolean = true;
    let obj: object = {};
    let arr: [] = [];
    let sym:symbol = Symbol("a");
    let und:undefined = undefined;
    let nul: null = null;
    let big: bigint = 100n;
}