1. 리액트 스타일링 방법

1.1 전통적인 방법으로 스타일링
1.1.1 인라인 스타일
1.1.2 글로벌(외부) 스타일
1.1.3 CSS 모듈(+ classnames 패키지) <- 특정 컴포넌트에만 css를 적용하는 방법

1.2 CSS-IN-JS 방법
1.2.1 styled-components💅
1.2.2 emotion 👩‍🎤 - 태그명을 그대로 쓰고 classname에 CSS를 적용 가능
styled components가 가지고 있는 기능을 지원. 모듈처럼 css 뒤에 암호가 붙는데, 같은 css를 쓰면 같은 암호가 붙는다.
1.2.2.1 CSS를 런타임에 생성한다.
1.2.2.2 초기 로딩에 대한 오버헤드가 크다.
1.2.3 Vanilla Extract(최근 떠오르는 방법)🧁 - (타입스크립트 지원이 완벽 + 제로 런타임) 이 강점
빌드하는 과정에서 CSS파일이 물리적으로 생김

1.3 Tainwind CSS
1.3.1 단독 유틸리티 퍼스트 CSS 라이브러리

1.4 그 외
1.4.1 이미지 렌더링
1.4.2 폰트 적용

CSS-IN-JS?
자바스크립트 파일 안에서 CSS를 작성하는 방법
한때 리액트에서 유행했던 방법

npm이 가장 범용적이다.

앵귤러 -> 리액트
앵귤러는 국내보다는 해외에서 많이 쓰인다.
리액트가 후발주자.

앵귤러JS가 가진 단점은 쳐내고 장점만 취한 것이 리액트

VUE: 앵귤러와 리액트의 장점을 가지고 있는 라이브러리
