type LoginLayout = {
  title: string;
  description: string;
  children: React.ReactNode;
};

type Placeholder = {
  placeholder: string;
};

type CheckboxLabel = {
  labelId: string;
};

type Button = {
  text: string;
  isBg: boolean;
};
