export default function App() {
  // JSX
  // JavaScript + XML - 확장 문법
  // 0. JSX는 HTML과 굉장히 유사하다.

  // 1. 하나의 루트 요소만 반환해야 한다.
  // 2. 모든 태그는 닫아줘야 한다.
  // 3. 태그의 속성은 카멜케이스로 사용해야 한다.
  // 4. 표현식(속성값)은 중괄호 안에서 사용할 수 있다.
  // 5. 인라인 스타일은 객체로 작성한다.
  // 6. 주석도 중괄호 안에 넣어야 한다.
  return (
    <div>{/* 주석! */}App</div>
  )
}